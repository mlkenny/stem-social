// Simple User Type (for importing to backend/frontend, will change later).
export type User = {
    id: string;
    email: string;
    name: string
};

// Simple Post Type.
export type Post = {
    id: string;
    title: string;
    content: string
};